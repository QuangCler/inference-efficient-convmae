# Core (CPU or CUDA). For an NVIDIA GPU install the matching torch build from
# https://pytorch.org FIRST (see the per-GPU table in README.md), then install the rest.
torch>=2.1
torchvision>=0.16
timm==0.9.16
gradio>=4.0
pillow
numpy

# Optional:
#   gdown            # fetch_checkpoints.py (download face checkpoints)
#   kaggle           # fetch_datasets.py (download the original datasets)
#   onnx tensorrt    # build_trt.py + the TensorRT backend in the UI (NVIDIA GPU only)
